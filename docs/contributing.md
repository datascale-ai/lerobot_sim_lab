# Contributing

- Use Python 3.10 or newer.
- Install development dependencies with `pip install -e ".[dev]"`.
- Run `ruff check src tests scripts` before submitting changes.
- Run `pytest` for automated verification.
- Keep new public functions and classes documented with English docstrings.
